# URL base del backend Spring Boot (ServicioDeUsuarios)
VITE_API_BASE_URL=http://localhost:8080/api

# true  -> usa el adaptador mock en localStorage (no requiere backend)
# false -> consume la API REST real
VITE_USE_MOCK=true
